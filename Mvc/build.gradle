apply plugin: 'java'

sourceCompatibility = 1.8
version = 'latest'

repositories {
    mavenCentral()
    mavenLocal()
}

dependencies {
    compile 'net.avacati.lib:jetty:0.0.1'

    testCompile group: 'junit', name: 'junit', version: '4.11'
}